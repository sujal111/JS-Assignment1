// Problem Statement: Create a variable.js file and declare variables and assign string, boolean, undefined and null data types, Display all the value with there type.

let firstName = "Subhadip";
let job = true;
let car;
let bike = null;

console.log(`Value of name is ${firstName} & type is ${typeof firstName}`);
console.log(`Value of job is ${job} & type is ${typeof job}`);
console.log(`Value of car is ${car} & type is ${typeof car}`);
console.log(`Value of bike is ${bike} & type is ${typeof bike}`);